○　NEO_BUG-3123 EMM自動表示メッセージが時間経過でも消去されない（DLNA経由の再生）
  → 161129
  * BS101に対して録画してみる
  * 録画したところ、普通に視聴中にEMMが表示される番組は、録画コンテンツとして再生するときにEMMの表示がない
    - 表示しよという要求(Intent)が来ない
  * 白箱も同様な動作
  * バグのタイトルと内容を修正した
    - NEO_BUG-3123 EMM自動表示メッセージが時間経過でも消去されない（録画再生時） に修正
  * バグ票にコメント文を追加し、現時点で検証できるチャンネルがわからないため、検証できない
  → 161201に移す
