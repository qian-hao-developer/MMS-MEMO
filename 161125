NEO_BUG-3119 #397 番組詳細の無い番組の番組詳細が更新されない
  → 161124
  * cursorのpositionやlongDescriptionなどを出すようなログを追加した上に、ログデータを取得した
    - 161125_eitLog
    - ニュースになると、Programから取得した情報は２つあり(EIT(sとp))、pのlongDescriptionが前番組からtuneせずに
      引き続いた場合であれば、更新されないよう
  * EITのpがあるため、常に、sよりpが優先に採用されているが、pにおけるlongDescriptionが更新されていないため、前番組
    の番組詳細がそのまま残っているのがバグの原因
  * ARIBを調査し、pにおけるlongDescriptionが更新されるかどうかを調べる
    - SEIログを取る
    - 更新されるのが仕様であれば、SEIにQA

NEO_BUG-3259 #424 番組情報を持たない番組の開始終了時刻の表記について（UI_不具合管理：96関連）
  * Gガイドなどデータサービスのような番組であれば、TvContentDataがほぼNull状態で返してくれる
  * event_id = -1 であれば、開始時刻など表示させないように修正していいと中野さんのご指摘
    - 実際にデバッグしてみると、event_idは0になっているよう
    - start_timeとend_timeが0になっているため、そっちに条件を追加するかと考慮している
  * 開始時刻と終了時刻の下にあるプログレスバーについて、時刻と同様に隠れたほうがいいか否かは山岡さんの回答待ち中
