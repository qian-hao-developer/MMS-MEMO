◎ NEO_BUG-5270 #814 受信確認画面で受信レベル他が表示されない
  * 受信確認画面(000-F06-D03, D20)で、周波数の不備によりBER値と受信レベルが取得できない場合、デフォールトとして「---」を表示するように修正した
  * 自動受信終了後のonResume()に、受信した周波数をOrmaBranchに更新する処理を追加した
  レビュー待ち

◎ NEO_BUG-5320 #824 機能待機設定画面にて、stbcoreの値を逆に書き込んでいる
  * 設定の機能待機では、ONとOFFが仕様通り(power_saving_modeはONであれば0、OFFであれば1)となっていることが確認できた
  * 初期設定の起動設定にある機能待機は仕様と逆になっている
  レビュー待ち

◎ NEO_BUG-4749 #485 機能待機設定の仕様について
  * 上記の5320と同件
  レビュー待ち

◎ NEO_BUG-4986 E201、E202エラー通知が、リモコンの戻るキー押下で消去できてしまう
  * NEO_BUG-5089 エラーダイアログがBackキーで消去される　と同件
  処理済みにし、小林さんに変更

◎ NEO_BUG-5475 ハードディスク機能待機のリマークが間違っている
  * 文言の修正
  レビュー待ち

◎ NEO_BUG-4252 #19392 同じ番組が放送されているにも関わらず、チャンネル上キー押下でMBS毎日が041->042->043と切り替わる
  * イベント共有対象をEITsのみ→EITs/p/fに修正した
  レビュー待ち

◎ NEO_BUG-5173 #797 スキャン時に自動で設定されるBSデジタルのチャンネル設定ができない
  * setPresetNumberはBSを強制的にリセットしているため、別のAPIを作成し、対象のみのAPIを使用するように修正した
  * 自動受信の地デジが受信後ではプリセット番号がリセットされていない問題も修正した
  レビュー待ち
